"""App extensions registry."""

from flask import Flask


def init_extensions(app: Flask) -> None:
    """Initialize Flask extensions."""
    # Placeholder for future extensions (e.g., cache, auth, db)
    return None
